apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: demo-list
  namespace: argocd
spec:
  # The list generator lets you enumerate a static set of elements (apps).
  generators:
    - list:
        elements:
          - app: nginx
            path: UI_Deployment_Approach/nginx     # path in repo for this app
          - app: online-shop
            path: ArgoCD_multiclusters/online-shop   # path in repo for this app
          - app: chaiapp
            path: ApplicationSet/chai-app      # path in repo for this app
  # Template that will be used to generate individual Application CRs
  template:
    metadata:
      # name template uses the element key 'app'
      name: '{{app}}-list'
    spec:
      project: default
      source:
        repoURL: https://github.com/yash252525/ArgoCD_Application_Deployments.git
        targetRevision: main
        path: '{{path}}'                # resolved from element
      destination:
        server: https://172.31.1.26:33893
        namespace: default
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
